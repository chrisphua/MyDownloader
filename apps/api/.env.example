# Copy to apps/api/.env for local development.
#
# Default values point at DynamoDB Local started by `docker compose up`.
# In production these are set automatically by CDK on the Lambda function.

TODOS_TABLE_NAME=todos-local
AWS_REGION=local
DYNAMODB_ENDPOINT=http://localhost:8000
PORT=3000
