{
  "name": "mydownloader",
  "private": true,
  "version": "0.1.0",
  "description": "Video downloader for YouTube, TikTok, Instagram, and more",
  "scripts": {
    "start": "npm run start --prefix apps/desktop",
    "make:mac": "npm run make:mac --prefix apps/desktop",
    "download-binaries": "npm run download-binaries --prefix apps/desktop"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
